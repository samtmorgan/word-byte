import React, { ReactElement } from 'react';

export default function Header(): ReactElement {
  return (
    <header>
      <nav>
        <a href="/">👾 Word Byte</a>
      </nav>
    </header>
  );
}
